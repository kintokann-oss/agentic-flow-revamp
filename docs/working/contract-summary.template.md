# Contract summary — TASK-XXX

> **be-dev** writes this after implementing backend routes. Human-readable IR for checkpoints and downstream agents.  
> **Machine OpenAPI:** run the app → `@profile:stack.backend.openapi_path` (generated by the framework — not a hand-edited YAML file).

## Paths added or changed

| Method | Path | Purpose |
|--------|------|---------|
| | | |

## Breaking changes

- <!-- none / list -->

## Verify (self-check)

- [ ] Paths match **Proposed tech & scope** in `plan.md`
- [ ] Request/response behaviors documented
- [ ] Matches implemented routes (compare `@profile:stack.backend.openapi_path` when app is running)

## Notes for fe-dev / be-testing-agent

<!-- defaults, auth, persistence hints; client paths for FE mocks -->
